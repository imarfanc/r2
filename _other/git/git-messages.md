# Commit messages

Generated by `deno task git:history` on 2026-08-12. 6 commits.

Regenerate rather than edit; git is the source of truth.

## ♻️🗂️ reshape(repo): move public, tests, and dev tools under src/

- commit: `89ef3509e1f75b7f8b9ae16b8b18b3f91f99bca4`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-12T04:39:10-05:00
- refs: HEAD -> main

```text
  ██████╗ ███╗   ██╗███████╗   ████████╗██████╗ ███████╗███████╗
 ██╔═══██╗████╗  ██║██╔════╝   ╚══██╔══╝██╔══██╗██╔════╝██╔════╝
 ██║   ██║██╔██╗ ██║█████╗        ██║   ██████╔╝█████╗  █████╗
 ██║   ██║██║╚██╗██║██╔══╝        ██║   ██╔══██╗██╔══╝  ██╔══╝
 ╚██████╔╝██║ ╚████║███████╗      ██║   ██║  ██║███████╗███████╗
  ╚═════╝ ╚═╝  ╚═══╝╚══════╝      ╚═╝   ╚═╝  ╚═╝╚══════╝╚══════╝
 ███████╗██████╗  ██████╗   ██████╗  ██████╗  ██████╗ ████████╗
 ██╔════╝██╔══██╗██╔════╝   ██╔══██╗██╔═══██╗██╔═══██╗╚══██╔══╝
 ███████╗██████╔╝██║        ██████╔╝██║   ██║██║   ██║   ██║
 ╚════██║██╔══██╗██║        ██╔══██╗██║   ██║██║   ██║   ██║
 ███████║██║  ██║╚██████╗   ██║  ██║╚██████╔╝╚██████╔╝   ██║
 ╚══════╝╚═╝  ╚═╝ ╚═════╝   ╚═╝  ╚═╝ ╚═════╝  ╚═════╝    ╚═╝
```

| Area             | What changed                                            |
| ---------------- | ------------------------------------------------------- |
| 📁 src/public/   | browser assets moved under src/                         |
| 🧪 src/tests/    | all tests consolidated from tests/ and _repo/tests/     |
| 🛠️ src/tools/    | dev CLIs (check, choose, dev, typecheck) left _repo/    |
| 🧩 src/shared/   | fs, paths, process, types lifted from _repo/tools/lib/  |
| 🗑️ _repo/skills/ | skills tree, skills.json, and its check removed         |
| 📜 _repo/adrs/   | ADR 0006 records the single source tree                 |
| ⚙️ deno.json     | task paths re-pointed, dev watcher excludes tools/tests |
| 📚 docs          | AGENTS.md, README.md, changelog follow the new layout   |
| 🍺 data/scripts/ | new daily1 brew-update script                           |

The root listed four first-party code directories with nothing in the naming to
say which were the program and which were scaffolding. Collapsing public/,
tests/, and the dev CLIs into src/ preserves every sibling import specifier, so
only string-named paths changed; _repo/ keeps the genuine repository operations
(rename, git:history) and ADR 0006 records the reasoning.

---

## Vorssaint

- commit: `8e705c35b2ca0b88b840742fb9652c81f6d03ee2`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-11T04:44:36-05:00
- refs: origin/main, origin/HEAD

---

## ✨ 📜 ADD(repo): write the git log to Markdown, and a markdown demo

- commit: `55893241ef5609bce5fb0e8f4e4b96db06a7d8ee`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-08T22:29:05-05:00

```text
 ██╗  ██╗██╗███████╗████████╗ ██████╗ ██████╗ ██╗   ██╗
 ██║  ██║██║██╔════╝╚══██╔══╝██╔═══██╗██╔══██╗╚██╗ ██╔╝
 ███████║██║███████╗   ██║   ██║   ██║██████╔╝ ╚████╔╝
 ██╔══██║██║╚════██║   ██║   ██║   ██║██╔══██╗  ╚██╔╝
 ██║  ██║██║███████║   ██║   ╚██████╔╝██║  ██║   ██║
 ╚═╝  ╚═╝╚═╝╚══════╝   ╚═╝    ╚═════╝ ╚═╝  ╚═╝   ╚═╝
```

| Area                    | Change                                                                                                                                                                            | Why                                                                                                                                                                                  |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `deno task git:history` | Reads the log and writes `_other/git/git-history.md` (a line per commit, grouped by day) and `git-messages.md` (every message in full), narrowed by `--limit`, `--since`, `--out` | The history should be readable from the working tree alone — by a reader browsing it, and by an agent that has the files and not the git database                                    |
| `lib/git-history.ts`    | Parses `git log` with `\x1e`/`\x1f` separators rather than newlines, and keeps the renderers pure                                                                                 | Commit bodies contain newlines, so the naive format loses them; the split makes rendering testable without a repository to read                                                      |
| `fenceArt`              | Detects a run of three or more box-drawing or ASCII-stroke rows and wraps it in a `text` fence, joining blocks across up to two blank lines                                       | Markdown renders a bare banner as a paragraph — proportional font, spaces collapsed, lines joined — the one presentation that destroys it; a two-word banner is one picture, not two |
| Commit bodies           | No longer `trim()`ed                                                                                                                                                              | A banner's first row is often indented a column further, and trimming sheared the top off the artwork                                                                                |
| `_other/git/**`         | Excluded from `deno fmt`                                                                                                                                                          | Generated prose; reflowing it is the wrong kind of tidy                                                                                                                              |
| `markdown-preview`      | New demo script rendering `sample.md` to ANSI with charmd, passing `lineWidth` explicitly                                                                                         | The renderer is a Deno module, so the output needs no glow or bat installed — and `Deno.consoleSize()` throws on the pipe the console hands every script                             |
| `.gitignore`            | Ignores `.fuse_hidden*`                                                                                                                                                           | Orphans a FUSE mount leaves behind when a still-open file is replaced                                                                                                                |
| ADR 0005                | Records the self-report decisions and what goes stale                                                                                                                             | Generated files committed on purpose is a choice worth explaining                                                                                                                    |

Checks were not run.

---

## ♻️ 🦕 PORT(scripts): move the TypeScript scripts to Deno, add daily1

- commit: `a62227ab9182dfb454e003fe1ccc297779fa2ac2`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-08T20:54:20-05:00

```text
 ██████╗ ███████╗███╗   ██╗ ██████╗
 ██╔══██╗██╔════╝████╗  ██║██╔═══██╗
 ██║  ██║█████╗  ██╔██╗ ██║██║   ██║
 ██║  ██║██╔══╝  ██║╚██╗██║██║   ██║
 ██████╔╝███████╗██║ ╚████║╚██████╔╝
 ╚═════╝ ╚══════╝╚═╝  ╚═══╝ ╚═════╝
```

| Area                | Change                                                                                                                                                            | Why                                                                                                                                                                      |
| ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `data/scripts/*.ts` | Eleven scripts and `_common.ts` move off `Bun.spawn`/`Bun.file`/`process` onto `Deno.Command` and friends, with a `#!/usr/bin/env -S deno run -A` shebang         | Two TypeScript runtimes for one language meant remembering which APIs a script was allowed, and Bun had to be installed for the part of `data/` most likely to be edited |
| `_process.ts`       | New shared module for running things — `spawn`, `exists`, `which`, `sleep`, `interactive`, `readLine` — keeping the Bun-shaped surface over a Deno implementation | Translating `"piped"`/`"null"`/`status` once made the port a change of runtime rather than a rewrite of every call site                                                  |
| `which()`           | Tool lookup walks `PATH` instead of spawning `["command", "-v", x]`                                                                                               | `command` is a shell builtin, so the spawn never worked — Bun failed quietly as "not installed" and `Deno.Command` throws                                                |
| `spawnCommand`      | `.ts` picks Deno when the shebang names Deno, Bun otherwise                                                                                                       | TypeScript is the one extension two runtimes both claim, so the file gets to say which it wants                                                                          |
| `daily1` group      | New group, now the default, with `brew-inventory` and `brew-update` and a page in both frontends                                                                  | Routine maintenance is what the runbook is opened for; setup is the one-time case                                                                                        |
| Console tabs        | The output/source toggle is gone; one strip runs `Output` first, then a tab per file, and the source loads with the script                                        | Two controls answered one question — which of these am I looking at                                                                                                      |
| `statusline.ts`     | Claude Code status line — model, context window, 5-hour and weekly limits — wired up in `.claude/settings.json`                                                   |                                                                                                                                                                          |
| ADR 0004            | Records the one-language-one-runtime decision and the seams it leaves                                                                                             | `data/` is exempt from fmt, lint and the structural checks, so nothing but running these scripts proves they work                                                        |

Checks were not run.

---

## ✨ 🖥️ ADD(setup): four setup scripts, and v2 restyled onto v1's layout

- commit: `8f7c54bbbbdcb43a5ea577d5e707021695bfcdd0`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-08T19:11:38-05:00

```text
 ███████╗███████╗████████╗██╗   ██╗██████╗
 ██╔════╝██╔════╝╚══██╔══╝██║   ██║██╔══██╗
 ███████╗█████╗     ██║   ██║   ██║██████╔╝
 ╚════██║██╔══╝     ██║   ██║   ██║██╔═══╝
 ███████║███████╗   ██║   ╚██████╔╝██║
 ╚══════╝╚══════╝   ╚═╝    ╚═════╝ ╚═╝
```

| Area               | Change                                                                                                                           | Why                                                                                                                                |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `setup2-power`     | Power settings move out of `setup-defaults.ts` into their own script, applying via `sudo -n` and printing the commands otherwise | `pmset` is not `defaults` and needs sudo, not Full Disk Access; the console has no terminal to prompt on, so it degrades to advice |
| `setup2-shortcuts` | Clears the Show Desktop hotkeys (36 and 37) through `plutil -replace -json` into `defaults`                                      | `-dict-add` writes an old-style plist with no number type, so the "no shortcut" triple lands as strings and the key keeps working  |
| `setup3-brew-*`    | An inventory script and an install script over one shared list, `data/scripts/_brew-packages.sh`                                 | A checker and an installer that disagree about what "installed" means is the bug the shared list exists to prevent                 |
| Hot corners        | Bottom-right corner set to 0 (no action) instead of Quick Note                                                                   | 0 is what the System Settings picker writes for "–"                                                                                |
| `v2` frontend      | Chip strip replaced by `v1`'s rail and console; the filter stays, the source view loads with the script instead of on demand     | The old arrangement read as a different application rather than a second design of the same one — a version is a restyling         |
| `isContentPath`    | Moved from `check.ts` to `paths.ts` and applied in `typecheck.ts`                                                                | `data/` scripts run under bun/uv/bash and were being type-checked as repository source                                             |
| `deno.lock`        | Dropped the `@std/assert` and `@std/cli` entries                                                                                 | Nothing imports them any more                                                                                                      |

Checks were not run.

---

## i

- commit: `a9d05561ef9a94d4d4e210722002fc3db0d63d26`
- author: imarfanc <142709234+imarfanc@users.noreply.github.com>
- date: 2026-08-08T00:06:32-05:00

---
